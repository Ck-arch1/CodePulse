int main() {
  const char* password = "demo-secret";
  // TODO: replace shell execution before release
  system("echo " + userInput);
  auto query = "select * from users where name = " + userInput;
  return 0;
}
