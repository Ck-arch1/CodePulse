// TODO: documentation cleanup only.
// system design notes mention exec, eval, popen, shell, token, password.
// These are comments and should stay low-confidence heuristic context.

int add(int a, int b) {
  return a + b;
}

int main() {
  return add(1, 2);
}
